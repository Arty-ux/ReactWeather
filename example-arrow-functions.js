var names = ['Arty','Ausra','Matt'];

/*names.forEach(function (name) {
  console.log('forEach', name);
});
console.log('-----');
names.forEach((name) => {
  console.log('arrowFunc', name);
});
console.log('-----');
names.forEach((name) => console.log(name));

console.log('-----');

var returnMe = (name) => name + '!';
console.log(returnMe('Arty'));*/

/*var person = {
  name: 'Arty',
  greet: function () {
    names.forEach((name) => {
      console.log(this.name + ' say hi to ' + name)
    });
  }
};

person.greet();
*/

// Chalange Area


// addStatment
var addStatment = (a,b) => {
  return a + b;
}

console.log(addStatment(4,7));
// addExpression

var addExpression = (a, b) => a + b;

console.log(addExpression(3,-2));

/*
function add(a,b) {
  return a+b;
}

console.log(add(1,3));
console.log(add(9,0));
 */
